To run the tests in this folder, cd to the root of the project and run
the following command:

pybot -A tests/conf/default.args tests

The report and log files will be placed in tests/results

